# SpringBootDynamoDbLocal
A hands-on practice project that integrates **Spring Boot 3.5** with **DynamoDB Local** (embedded, in-memory).   No AWS account required — the database starts inside the JVM alongside the application.
